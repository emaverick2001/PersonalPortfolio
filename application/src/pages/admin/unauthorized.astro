---
import AdminLayout from "@layouts/AdminLayout.astro"
import { SignOutButton } from "@clerk/astro/react"
---

<AdminLayout title="Unauthorized">
  <div class="space-y-4">
    <div>
      <p class="text-xs uppercase tracking-[0.35em] text-zinc-500">Admin</p>
      <h1 class="text-3xl font-semibold tracking-tight text-white">Access denied</h1>
      <p class="mt-2 text-sm text-zinc-400">
        Your account is signed in, but it is not allowlisted for admin access.
      </p>
    </div>

    <div class="rounded-2xl border border-zinc-800 bg-zinc-950/50 p-5 text-sm text-zinc-400">
      If this is unexpected, confirm your user ID or email allowlist in <code>.env</code> and restart
      the dev server.
    </div>

    <SignOutButton redirectUrl="/" client:load>
      <button
        type="button"
        class="rounded-full border border-zinc-700 px-4 py-2 text-xs uppercase tracking-[0.3em] text-zinc-200 transition hover:border-zinc-500 hover:text-white"
      >
        Sign out
      </button>
    </SignOutButton>
  </div>
</AdminLayout>
